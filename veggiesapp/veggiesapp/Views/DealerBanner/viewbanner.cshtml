
@{
    ViewBag.Title = "viewbanner";
    Layout = "~/Views/Shared/MyLayout  dealer.cshtml";
}

@model List<veggiesapp.Models.homebanner>

<div class="row">
    <div class="col-xl-12 col-lg-12 col-md-12 col-sm-12 col-12">
        <div class="page-header">
            <h2 class="pageheader-title">Banner Management</h2>
            <p class="pageheader-text">Proin placerat ante duiullam scelerisque a velit ac porta, fusce sit amet vestibulum mi. Morbi lobortis pulvinar quam.</p>
            <div class="page-breadcrumb">
                <nav aria-label="breadcrumb">
                    <ol class="breadcrumb">
                        <li class="breadcrumb-item"><a href="#" class="breadcrumb-link">Dashboard</a></li>
                        <li class="breadcrumb-item"><a href="#" class="breadcrumb-link">Banner Management</a></li>
                        <li class="breadcrumb-item active" aria-current="page">View Banner</li>
                    </ol>
                </nav>
            </div>
        </div>
    </div>
</div>

<div class="row">
    <!-- ============================================================== -->
    <!-- basic table  -->
    <!-- ============================================================== -->
    <div class="col-xl-12 col-lg-12 col-md-12 col-sm-12 col-12">
        <div class="card">
            <h5 class="card-header" style="background-color:#71748d;color:white">Banners</h5>
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table table-striped table-bordered first">
                        <thead>
                            <tr>
                                <th>Image</th>
                                <th>Visible</th>
                                <th>Edit</th>
                                <th>Delete</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var ban in Model)
                            {
                                <tr>
                                    <td><img src="@(ban.imagepath)" width="400px" height="174px"></td>

                                    @if (@ban.visible == true)
                                    {
                                        <td style="text-align:center">Yes</td>
                                    }
                                    else
                                    {
                                        <td style="text-align:center">No</td>
                                    }

                                    <td style="text-align:center"><a href=@Url.Action("editbanner","DealerBanner",new {id=ban.id})><input type="button" value="Edit" style="background-color: #5bc0de;color: white;width: 74px;height: 30px;"></a></td>
                                    <td style="text-align:center"><a href=@Url.Action("deletebanner","DealerBanner",new {id=ban.id})><input type="Button" value="Delete" onclick="return confirm('are you sure you want to Delete this Banner?');" style="background-color: red;color: white;width: 74px;height: 30px;"></a></td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
    <!-- ============================================================== -->
    <!-- end basic table  -->
    <!-- ============================================================== -->
</div>

